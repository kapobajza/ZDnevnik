{
  "version": "0.2.0",
  "configurations": [
    {
      "command": ".apps/web/node_modules/.bin/astro dev",
      "name": "Development server",
      "request": "launch",
      "type": "node-terminal"
    },
    // Run script: api:test with arguments
    {
      "command": "pnpm --filter api test -- -t \"return 200 if a teacher tries to access list\"",
      "name": "API test",
      "request": "launch",
      "type": "node-terminal"
    }
  ]
}
